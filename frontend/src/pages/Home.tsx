import { Box, Typography } from "@mui/material";

export default function Home() {
  return (
    <Box>
      <Typography variant="h4" align="center" mt={4}>
        🎉 ApplyFlow is running!
      </Typography>
      </Box>
  );
}